let count = 0;

// while = enquanto.
// Se não colocar uma condição de parada o  while irá rodar eternamente ou até travar.
while (count < 10) {
    console.log(`O número da vez é ${count}`);
    count++;
}